# WhisperVault Environment Variables Example
# Copy this file to .env, .env.production, or .env.test and fill in secrets as needed

# --- Database ---
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/whispervault

# --- JWT ---
JWT_SECRET=your_jwt_secret_here

# --- Email ---
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email_user
EMAIL_PASS=your_email_password
EMAIL_FROM="WhisperVault <noreply@yourdomain.com>"

# --- TOTP ---
TOTP_ISSUER=WhisperVault

# --- Twilio SMS ---
TWILIO_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=auth_token_here
TWILIO_PHONE_NUMBER=example_phone_number

# --- Other ---
NODE_ENV=development

# For production, use .env.production and set NODE_ENV=production
# For tests, use .env.test and set test DB credentials
